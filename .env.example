# Installed-app OAuth (for local interactive auth)
# Path to your OAuth Client (Desktop app) JSON
GSC_CLIENT_SECRETS=/absolute/path/to/client_secrets.json
# Where to cache the user token (created after first login)
GSC_TOKEN_PATH=./.gsc_token.json

# Service Account (for server/CI headless auth)
# Path to your Service Account JSON key
GSC_SA_KEY=/absolute/path/to/analytics-deal-scale.json
# Scope: use readonly unless you need write ops (sitemaps add/delete)
GSC_SCOPE=https://www.googleapis.com/auth/webmasters.readonly

# Target property identifier (must match property type EXACTLY)
# - URL-prefix property example (note trailing slash): https://dealscale.io/
# - Domain property example: sc-domain:dealscale.io
GSC_SITE_URL=https://dealscale.io/

# Optional HTTP settings
GSC_HTTP_TIMEOUT=30
GSC_USER_AGENT=DealScale-GSC-SDK/1.0